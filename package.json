{
  "name": "leadops-outreach-automator",
  "version": "1.0.0",
  "private": true,
  "description": "LeadOps Outreach Automator — leads + Gmail campaign sending",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "npm start --prefix backend",
    "dev:backend": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
