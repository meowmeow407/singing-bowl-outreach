# Frontend (Singing Bowl Outreach)

Run the frontend from the repo root instructions in `../README.md`.

This frontend proxies `/api/*` to the backend at `http://localhost:5001` (see `vite.config.js`).
